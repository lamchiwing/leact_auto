// src/services/geminiService.ts
// Temporary safe stub to make build pass.
// Replace with Worker API call later.

export async function getConsultationResponse(prompt: string): Promise<string> {
  // TODO: change to call your backend, e.g. /api/consult
  return `AI service not configured yet. Your message: ${prompt}`;
}
